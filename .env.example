# Google Cloud service account JSON key (local path only — never commit the key file)
GOOGLE_SERVICE_ACCOUNT_FILE=path/to/service-account.json

# Workspace super admin to impersonate (domain-wide delegation)
GOOGLE_ADMIN_EMAIL=admin@getinmarketing.site
